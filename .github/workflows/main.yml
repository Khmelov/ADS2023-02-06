name: PR Validation

on: [pull_request]

jobs:
  validate_pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Validate PR
        run: |

          CHANGED_FILES=$(git diff --name-only main HEAD)

